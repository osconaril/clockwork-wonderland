/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // We use a custom server, so tell Next.js not to start its own
  // No special config needed beyond this
};

module.exports = nextConfig;
